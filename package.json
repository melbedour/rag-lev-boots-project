{
  "name": "consulting-report-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "public",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w public\" \"npm run dev -w server\"",
    "build": "npm run build -w public && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "concurrently": "^9.2.1",
    "globals": "^15.14.0",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@google/genai": "^1.21.0",
    "openai": "^6.0.1",
    "pdf-parse": "^1.1.1",
    "pdf-ts": "^0.0.2"
  }
}
